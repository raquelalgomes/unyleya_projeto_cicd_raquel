trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Docker Hub'
    repository: 'raquelalgomes/mobead'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
